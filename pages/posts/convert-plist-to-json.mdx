import { Post } from '../../components/post'
import { Tags } from '../../lib'

export const meta = {
  author: 'Ethan Mick',
  title: 'Convert Plist to JSON',
  tags: [],
  createdAt: '2017-03-03T08:00:00.000Z',
}

export default ({ children }) => <Post {...meta}>{children}</Post>

As long as you have Xcode installed, you can simply do:

```bash
plutil -convert json data.plist -o data.json
```
