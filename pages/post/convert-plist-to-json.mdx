import withPost from '../../components/with-post'

export const meta = {
  type: 'POST',
  title: 'Convert Plist to JSON',
  author: 'Ethan Mick',
  slug: 'convert-plist-to-json',
  excerpt: '',
  createdAt: '2019-11-22T08:00:00.000Z'
}

export default withPost({ ...meta })

As long as you have Xcode installed, you can simply do:

    plutil -convert json data.plist -o data.json
